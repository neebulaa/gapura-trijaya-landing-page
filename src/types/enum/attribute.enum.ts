export enum AttributeTypeEnum {
  Text = 'text',
  Textarea = 'textarea',
  Price = 'price',
  Boolean = 'boolean',
  Select = 'select',
  Datetime = 'datetime',
  Date = 'date',
}

export enum BooleanOptionsEnum {
  Yes = 1,
  No = 0,
}

export enum ValidationEnum {
  Number = 'number',
  Decimal = 'decimal',
  Email = 'email',
  URL = 'url',
}
